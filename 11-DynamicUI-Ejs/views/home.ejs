<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <!-- <link rel="stylesheet" href="home.css"> -->
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body>
    <div class="bg-blue-100 m-2 p-2 ">
      <nav class="m-4 text-center">
        <a
          class="text-center bg-amber-800 p-1 rounded-sm border-2 border-blue-600 hover:bg-red-400"
          href="/host/add-home"
          >Add Home</a
        >
      </nav>
      <header
        class="text-center mt-6 p-3 h-40 text-cyan-50 bg-amber-950 content-center border-2 rounded-2xl hover:bg-red-800"
      >
        <h1 class="font-bold text-4xl">Resister House Name are:</h1>
      </header>
      <main>
        <ul class="text-center mb-8 p-4 bg-amber-600  ">
          <% registerHome.forEach((homes)=>{ %> 
          <li class=" text-2xl font-bold"><%= homes.houseName %></li>
          <% }) %>
        </ul>
      </main>
    </div>
  </body>
</html>
